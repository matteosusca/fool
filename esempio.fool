let
  var x:int = 5+3;
  fun f:bool (n:int, m:int)
    let 
      var x:bool = true;
    in x==(n==m);
  fun g:int (b:bool, b2:bool, b3:bool)
    if (b)
      then {1}
      else {0};
in  
  print ( 
   g(2, true)
  )
;
